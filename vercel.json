{
  "version": 2,
  "functions": {
    "api/[...proxy].js": {
      "runtime": "@vercel/node"
    }
  },
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/[...proxy].js" },
    { "src": "/(.*)", "dest": "/frontend/$1" }
  ]
}
